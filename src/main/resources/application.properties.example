spring.application.name=marketplace
spring.datasource.url=jdbc:mysql://localhost:3306/marketplace?createDatabaseIfNotExist=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root

spring.datasource.password=TU_PASSWORD_AQUI
spring.jpa.hibernate.ddl-auto=update

# Subida de archivos (imagenes de vehiculo). El default de Spring es 1MB.
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT
# La clave debe tener MIN 32 caracteres (256 bits) por usar HMAC-SHA256.
jwt.secret-key=CAMBIAR_POR_UNA_CLAVE_DE_AL_MENOS_32_CARACTERES
# Duracion del token en milisegundos (86400000 = 24 horas)
jwt.expiration=86400000

# Mercado Pago (Checkout Pro)
# Access Token de produccion del usuario vendedor DE PRUEBA (APP_USR-...). Mejor pasarlo por variable de entorno.
mercadopago.access-token=${MERCADOPAGO_ACCESS_TOKEN:}
# URL publica HTTPS del backend (ngrok/cloudflared) para la back_url. Mercado Pago no acepta localhost.
app.public-url=${APP_PUBLIC_URL:}
# URL del frontend a la que se redirige al volver de Mercado Pago.
app.frontend-url=${APP_FRONTEND_URL:http://localhost:5173}